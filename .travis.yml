language: ruby
rvm:
  - 2.5.8
cache:
  - bundler
before_script:
  — bundle exec bundle install --jobs=3 --retry=3
script:
  — bundle exec rake db:create
  — bundle exec rake db:schema:load
  — bundle exec rspec
