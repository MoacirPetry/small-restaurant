<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta http-equiv="x-ua-compatible" content="ie=edge">
    <title>Small Restaurant</title>
    <%= csrf_meta_tags %>
    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.3.1/css/all.css" integrity="sha384-mzrmE5qonljUremFsqc01SB46JvROS7bZs3IO2EmfFsd15uHvIt+Y8vEf7N7fWAU" crossorigin="anonymous">
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>

  </head>

  <body>
    <nav class="site-header sticky-top py-1">
      <div class="container d-flex flex-column flex-md-row justify-content-between">

        <%= link_to root_path, class: "py-2" do %>
          <% if false %>
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="d-block mx-auto"><circle cx="12" cy="12" r="10"></circle><line x1="14.31" y1="8" x2="20.05" y2="17.94"></line><line x1="9.69" y1="8" x2="21.17" y2="8"></line><line x1="7.38" y1="12" x2="13.12" y2="2.06"></line><line x1="9.69" y1="16" x2="3.95" y2="6.06"></line><line x1="14.31" y1="16" x2="2.83" y2="16"></line><line x1="16.62" y1="12" x2="10.88" y2="21.94"></line></svg>
          <% end %>
          <i class="fas fa-utensils"></i>
          <i class="fas fa-home"></i>
        <% end %>
        <a class="py-2 d-none d-md-inline-block" href="#">Order</a>
        <%= link_to 'Tables', tables_path, class: "py-2 d-none d-md-inline-block" %>
        <%= link_to 'Categories', categories_path, class: "py-2 d-none d-md-inline-block" %>
        <a class="py-2 d-none d-md-inline-block" href="#">Product</a>
        <a class="py-2 d-none d-md-inline-block" href="#">Customer</a>

        <% unless user_signed_in? %>
          <%= link_to new_user_session_path, class: "py-2 d-none d-md-inline-block" do %>
            <i class="fas fa-sign-in-alt"></i>
            <i class="fas fa-user-tie"></i>
          <% end %>
        <% else %>
          <%= link_to destroy_user_session_path, method: :delete, class: "py-2 d-none d-md-inline-block" do %>
            <i class="fas fa-user-tie"></i>
            <i class="fas fa-sign-out-alt"></i>
          <% end %>
        <% end %>

      </div>
    </nav>

    <div class="container">

      <% if notice %>
        <div id="flash">
          <div class="alert alert-success">
            <%= flash[:notice] %>
          </div>
        </div>
      <script>
        setTimeout();
      </script>
      <% end -%>

      <%= yield %>

    </div>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </body>
</html>
